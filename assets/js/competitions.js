import { supabaseClient } from './supabase-client.js'; import { protectAndRender } from './session-ui.js'; const $=x=>document.getElementById(x),esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
(async()=>{const p=await protectAndRender('login.html');if(!p)return;const {data,error}=await supabaseClient.from('competitions').select('*').eq('status','published').order('opens_at');if(error){$('status').hidden=false;$('status').textContent=error.message;return}const n=Date.now();$('competitionList').innerHTML=(data||[]).map(c=>{const o=new Date(c.opens_at).getTime(),z=new Date(c.closes_at).getTime(),s=n<o?'Upcoming':n>z?'Closed':'Live',href=s==='Live'?`competition.html?id=${c.id}`:`competition-dashboard.html?id=${c.id}`;return `<article class="module-admin-card"><span class="status-pill ${s.toLowerCase()}">${s}</span><h2>${esc(c.title)}</h2><p>${esc(c.description||'')}</p><div class="module-admin-meta"><span>${new Date(c.opens_at).toLocaleString()}</span><span>${Math.round(c.duration_seconds/60)} min</span></div><a class="primary-btn" href="${href}">${s==='Live'?'Start Competition':s==='Closed'?'View Results':'View Countdown'}</a></article>`}).join('')||'<div class="empty-state">No published competitions.</div>';})();
